#include "main.h"

/**
 * print_env -prints a string in reverse
 * @s: strig to be printed
 */
void print_rev(char *5)
{
	int i = 0;
	
	while (s[i] != '\0')
		i++;
	for (i = i -1; i >= 0; i--)
		_putchar(s[i]);

	_putchar('\n');
}
