#include "main.h"

/**
 * _abs - Computes the sbsolute valuse of an integer
 * @s: scans for integer
 * Return: absolute value
 */

int _abs(int s)
{
	if (s >= 0)
		return (s);
	else
		return (-s);
}
