#include "main.h"

/**
 * add - Add two integers by function.
 * @i: First integer variable to add.
 * @i: First integer variable to add.
 * Return: return with summation.
 */
int add(int i, int l)
{
	int n;
	 n = i + l;
	return (n);
}
