#include "main.h"
/**
 * main: first function
 * Return: Returns 0
 * print_alphabet: function
 */

void print_alphabet(void)
{
	char ch;

	for (ch = 'a' ; ch <= 'z' ; ch++)

	_putchar(ch);
	_putchar('\n');
}
